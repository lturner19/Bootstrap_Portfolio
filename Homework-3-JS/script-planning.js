
var password = generatePassword();
var passwordText = document.querySelector("#password");

//passwordText.value = password;
//  copyBtn.removeAttribute("disabled");
//copyBtn.focus();


//function copyToClipboard() {
// BONUS



getRandomLower()

function getRandomUpper() {
  var beta = upper[Math.floor(Math.random() * upper.length)];
  console.log(beta);
}

getRandomUpper()


function getRandomNum() {
  var nonAlpha = number[Math.floor(Math.random() * number.length)];
  console.log(nonAlpha);
}

getRandomNum()

function getRandomSymbol() {
  var symbols = "~!@#$%^&*()_+/><"
  console.log(symbols[Math.floor(Math.random() * symbols.length)]);
}

getRandomSymbol()


if (isLowerCase === true) {

}
var isUpperCase = confirm("Would you like to use capital letters?");
if (isUpperCase === true)
  for (beta = 0; beta < charcNumber.length; beta++) {
  }


var isNumber = confirm("Would you like to use numbers?");
if (isNumber === true)
  for (gamma = 0; gamma < charcNumber.length; beta++) {
  }


var isSymbol = confirm("Would you like to use symbols?");
if (isSymbol === true)
  for (delta = 0; delta < charcNumber.length; delta++) {
  }
  * /

function getRandomLower() {
  var alpha = lower[Math.floor(Math.random() * lower.length)];
  console.log(alpha);
}

getRandomLower()

function getRandomUpper() {
  var beta = upper[Math.floor(Math.random() * upper.length)];
  console.log(beta);
}

getRandomUpper()


function getRandomNum() {
  var nonAlpha = number[Math.floor(Math.random() * number.length)];
  console.log(nonAlpha);
}

getRandomNum()

function getRandomSymbol() {
  var symbols = "~!@#$%^&*()_+/><"
  console.log(symbols[Math.floor(Math.random() * symbols.length)]);
}

getRandomSymbol()
